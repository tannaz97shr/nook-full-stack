repo: tannaz97shr/nook-full-stack
branch: main

## Last sync

date: 2026-09-01T00:20:00Z

### Updated in this project

- Read the repo: it is a bare `create-next-app` scaffold (default `page.tsx`, empty Tailwind config), so nothing upstream to match yet.
- Designed the full Nook product from scratch as `Nook.dc.html` — 13 screens plus a design-token spec page.
- Token set authored as CSS custom properties with a paste-ready Tailwind v4 `@theme` block for `src/app/globals.css`.
- Cafe photography imported from the user's uploads into `img/`.

## Screen map

| Screen | Repo files |
| --- | --- |
| All screens (Home, Menu, Cart, Confirmation, Gallery, About, Contact, Auth, Account, Orders, Rewards, Admin ×3) | *(not yet in repo — `src/app/page.tsx` is still the scaffold)* |
| Design tokens page | target: `src/app/globals.css`, `tailwind.config.ts` |
