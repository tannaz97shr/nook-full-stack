/** Natural-slug doc IDs for admin-created Category/MenuItem/OptionGroup/Option docs. */
export function slugify(input: string): string {
  return input
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "");
}
