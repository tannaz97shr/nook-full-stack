/** Single source of truth for menu Firestore collection names. */
export const MENU_COLLECTIONS = {
  categories: "categories",
  menuItems: "menuItems",
  optionGroups: "optionGroups",
  options: "options",
} as const;
